package Puyoo

import scalafx.application.JFXApp
import scalafx.scene.Scene

class Main extends JFXApp {
  stage = new JFXApp.PrimaryStage {
    title = "Puyo"
    scene = new Scene(180,360) {
 
    }
  }
 
}